---
interface Props { title: string; }
const { title } = Astro.props;
---

<header class="page-headline">
  <span class="eyebrow" aria-hidden="true">/</span>
  <h1>{title}</h1>
  <slot />
</header>

<style>
  .page-headline {
    position: relative;
    max-width: 1000px;
    padding-block: 0 clamp(30px, 4vw, 44px);
    border-bottom: 1px solid var(--color-border);
    text-align: start;
  }
  .eyebrow {
    display: block;
    margin-bottom: 12px;
    color: var(--color-theme);
    font-family: var(--font-mono);
    font-weight: 700;
  }
  h1 {
    color: var(--color-main);
    font-size: clamp(2.6rem, 5vw, 4.4rem);
    font-weight: 800;
    letter-spacing: -0.055em;
    line-height: 1;
  }
  .page-headline :global(p) {
    max-width: 680px;
    margin-top: 20px;
    color: var(--color-muted);
    font-size: 1.05rem;
    line-height: 1.7;
  }
  @media (max-width: 640px) {
    .page-headline { padding-block-end: 28px; }
    h1 { font-size: clamp(2.2rem, 10vw, 3.1rem); line-height: 1.05; }
    .page-headline :global(p) { margin-top: 16px; font-size: 1rem; }
  }
</style>
